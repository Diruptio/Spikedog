plugins {
    id 'java'
}

dependencies {
    compileOnly project(':')
}

jar {
    archiveFileName = 'reload.jar'
    doLast {
        copy {
            from jar.archiveFile
            into rootProject.file('run/modules')
        }
    }
}